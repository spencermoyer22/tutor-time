//insert resolvers here

const resolvers = {
    Query: {
        /* insert thing here */: async () => {
            return await /* thing */.find();
        },
    },
};

module.exports = resolvers;
